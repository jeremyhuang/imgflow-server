services:
  image-service:
    build: .
    container_name: imgflow-server
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data   # SQLite 資料庫持久化，容器重建後資料不遺失
    mem_limit: 1g
